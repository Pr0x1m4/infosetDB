infoset:
  build: .
  ports:
    - "6000:6000"
  links:
    - redis
  hostname: localhost
  environment:
    PYTHONPATH: /infoset-ng
redis:
  hostname: localhost
  image: redis:latest
  ports:
    - "6379:6379"